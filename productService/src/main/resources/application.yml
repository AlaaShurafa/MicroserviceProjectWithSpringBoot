server:
  port: 9093

spring:
  application:
    name: PRODUCT-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8889/productDB?serverTimezone=UTC
    username: root
    password: root
    nitialization-mode: always
  jpa:
    hibernate:
      ddl-auto:
        update  ddl-auto: update